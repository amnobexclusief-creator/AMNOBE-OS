# API Design

Guidelines:
- Gebruik OpenAPI voor contracten
- Versiebeheer: `/v1/` endpoints
- Auth: JWT + OAuth for external integrations

Voorbeelden:
- `GET /v1/projects/{id}`
- `POST /v1/memory/{type}`

Documentatie: genereer automatisch uit OpenAPI specs.

